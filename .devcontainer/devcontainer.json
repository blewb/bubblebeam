{
	"name": "Alpine",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"postCreateCommand": "ip link set dev eth0 mtu 1500 || ip link set dev $(ip route show default | awk '/default/ {print $5}') mtu 1500",
	"remoteUser": "root",
	"runArgs": [
        "--cap-add=NET_ADMIN"
    ]
}
